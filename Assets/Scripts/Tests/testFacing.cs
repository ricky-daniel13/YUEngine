using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using YU2.Splines;

[ExecuteAlways]
public class testFacing : MonoBehaviour
{
    /*public BezierThing spline;
    [Range(0f, 1f)]
    public float time = 0.01f;
    // Update is called once per frame
    /*void Update()
    {
        if(spline)
        {
            transform.rotation = Quaternion.LookRotation(spline.GetDirection(time), spline.GetNormal(time));
            transform.position = spline.GetPoint(time);
        }
    }*/
    /*private void OnValidate()
    {
        if (spline)
        {
            transform.rotation = Quaternion.LookRotation(spline.GetDirection(time), spline.GetNormalVerbose(time));
            transform.position = spline.GetPoint(time);
        }
    }*/
}
